[project]
name = "apis-main_controller"
version = "4.5.10"
description = "APIS Main Controller"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "wheel>=0.42.0",
    "bottle>=0.13.2",
    "netifaces>=0.11.0",
    "pytz>=2024.1",
    "requests>=2.31.0"
]

[dependency-groups]
dev = [
    "ipython",
    "ruff",
]

[tool.uv]
package = false